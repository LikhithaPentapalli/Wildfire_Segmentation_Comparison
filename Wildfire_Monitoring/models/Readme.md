## Pretrained Models

The trained model weights are not included in this repository due to GitHub file size limitations.

The models can be reproduced by running the training notebook using the FLAME dataset.
